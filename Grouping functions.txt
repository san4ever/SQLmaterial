SELECT COUNTRY, SUM(SALARY)
FROM EMP
GROUP BY COUNTRY

UNION ALL

SELECT NULL,SUM(SALARY)
FROM EMP


SELECT COUNTRY,SUM(SALARY)'
FROM EMP
GROUP BY ROLLUP(COUNTRY)  // output will be like india 250 china 450  null 700


SELECT COUNTRY,SUM(SALARY)'
FROM EMP
GROUP BY GROUPING SETS{

 (COUNTRY) //till here it is same as GROUP BY
 ()


}


